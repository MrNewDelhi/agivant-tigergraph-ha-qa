[pytest]
testpaths = tests
addopts = -ra
markers =
    functional: normal-operation functional tests (FT1-FT12)
    database: data-correctness tests (DT1-DT7)
    messy: messy / edge-case / larger-data tests (MD1-MD12)
